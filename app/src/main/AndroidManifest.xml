<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ActivitiesRoadmap"
        tools:targetApi="31">

        <activity
            android:name=".presentation.screens.activities.single_top.SingleTopActivity"
            android:label="@string/single_top_activity_label"
            android:launchMode="singleTop" />

        <activity
            android:name=".presentation.screens.activities.activity_b.ActivityB"
            android:label="@string/activity_b_label" />

        <activity
            android:name=".presentation.screens.activities.activity_a.ActivityA"
            android:exported="true"
            android:label="@string/activity_a_label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>